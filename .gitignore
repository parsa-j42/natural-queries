# Raw source data — never commit. The Access DB is multi-GB; the cleaned,
# browser-ready artifact (Parquet/SQLite) is built from it via the backend ETL.
*.mdb
*.accdb

# Derived data artifacts (regenerated by ETL; keep out of git history)
*.parquet
*.duckdb
data/raw/
data/build/
