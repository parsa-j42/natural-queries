# Copy to .env.local for local dev (gitignored by Vite's defaults).

# Base URL of the FastAPI backend that generates SQL.
# Defaults to http://localhost:8000 when unset.
VITE_API_URL=http://localhost:8000
